<div class="text-center">
  <h4 class="font-['Satoshi',system-ui,sans-serif] font-semibold text-sm text-slate-500 mb-3"><%= title %></h4>
  <div class="inline-block bg-white rounded-lg border border-slate-200 p-5">
    <%= radar_chart_svg(exercises, size: title.include?("Upper") ? 320 : 280) %>
  </div>
</div>
